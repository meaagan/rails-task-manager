<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task|%>
    <li><%= link_to task.title, task_path(task)%> - <%= link_to "Remove", task_path(task), method: :delete %></li>
  <% end%>
</ul>

<p><%= link_to "New Task", new_task_path %></p>
